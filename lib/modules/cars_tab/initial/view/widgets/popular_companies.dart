import 'package:flutter/cupertino.dart';

class PopularCompanies extends StatelessWidget {
  const PopularCompanies({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
